# Copy & rename to .env

# Spring application
APP_PORT=

# SMTP
EMAIL_HOST=
EMAIL_PORT=465
EMAIL_USERNAME=
# For docker-compose, use "" (EMAIL_PASSWORD="..."), for local run do not use the quotes
# Escape special characters with \
EMAIL_PASSWORD=

# FTP
FTP_HOST=
FTP_PORT=21
FTP_USERNAME=
# For docker-compose, use "" (FTP_PASSWORD="..."), for local run do not use the quotes
# Escape special characters with \
FTP_PASSWORD=
FTP_REMOTE_DIRECTORY=
FTP_LOCAL_DIRECTORY=